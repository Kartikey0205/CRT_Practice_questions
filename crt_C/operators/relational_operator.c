#include <stdio.h>
int main()
{
    // printf takes input then first process it and as a output number of alphabetit prints
    if (printf("India"))
    {
        printf("\nHello\n");
    }

    printf("%d", printf("%d", printf("ABCDEFGHIJ")));
    printf("%d", printf("\n%d ", printf("ABCDEFGHIJ")));
    return 0;
}